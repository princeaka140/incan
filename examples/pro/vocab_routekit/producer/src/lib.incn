"""
Producer library for the pro-level vocab companion example.

The consumer example imports this library through `pub::routekit`, which activates the sibling vocab companion metadata
during `incan build --lib`.
"""

pub from info import routekit_name
