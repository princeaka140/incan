# Cargo Deny Configuration
# See: https://embarkstudios.github.io/cargo-deny/

[advisories]
# Security vulnerability database checks
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
# cargo-deny v0.16+ expects `unmaintained` to specify the *scope* of the check.
# Valid values: ["all", "workspace", "transitive", "none"].
unmaintained = "transitive"
yanked = "warn"
ignore = [
    # transitive-only; no safe upgrade currently available, tracked in GitHub issue #260
    "RUSTSEC-2023-0089", # atomic-polyfill
]

[licenses]
# License policy
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "CC0-1.0",
    "ISC",
    "Unicode-3.0",
    "Zlib",
]
confidence-threshold = 0.8

[licenses.private]
# Ignore private workspace members
ignore = false
registries = []

[bans]
# Dependency bans
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
allow = []
deny = []
skip = []
# Note on remaining duplicate warning:
# `unicode-width` currently appears in two versions due to upstream constraints in `miette`/`textwrap`
# (and dev-path `wat`/`wast`). We intentionally keep this warning visible instead of suppressing it,
# so dependency drift remains observable until upstream converges.
skip-tree = [
    # Upstream-locked duplicate trees:
    # - tower-lsp 0.17 pulls lsp-types/bitflags v1
    # - rust-analyzer crates (ra_ap_*) pull older hashbrown/foldhash/winnow lines
    # - wasmtime/wasi stack pulls a separate native/runtime dependency graph
    { name = "tower-lsp", version = "0.17.0" },
    { name = "ra_ap_load-cargo", version = "0.0.325" },
    { name = "wasmtime-wasi", version = "43.0.1" },
]

[sources]
# Source repository checks
unknown-registry = "warn"
unknown-git = "warn"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []

[sources.allow-org]
# Allow GitHub organizations
github = []
gitlab = []
bitbucket = []
