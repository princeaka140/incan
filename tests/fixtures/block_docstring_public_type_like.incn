pub model CliModelProbe:
    """
    Line A documents the class API.
    Line B keeps interior newlines after trim().
    """
    id: int

pub class CliClassProbe:
    """
    Line A documents the class API.
    Line B keeps interior newlines after trim().
    """
    pub x: int

pub enum CliEnumProbe:
    """
    Line A documents the class API.
    Line B keeps interior newlines after trim().
    """
    V

pub trait CliTraitProbe:
    """
    Line A documents the class API.
    Line B keeps interior newlines after trim().
    """
    def meth(self) -> None: ...

pub type CliNewtypeProbe = newtype int:
    """
    Line A documents the class API.
    Line B keeps interior newlines after trim().
    """

    def zero(self) -> int:
        return 0
