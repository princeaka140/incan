"""Codegen snapshot: std.web.request and serde model (web extractors)

`std.web.request.Query` is a direct re-export of Axum's `Query`; it is not an Incan newtype, so this file does not exercise
`@derive(FromRequestParts)` or `@rust.delegate`. The snapshot still anchors stdlib web request types and a `Deserialize`
model as used in handler-style examples.

Note: Codegen verification only; not a runtime integration test.
"""

# Re-exported Axum extractor (implements FromRequestParts in Rust without Incan-side delegation)
from std.web.request import Query

from rust::serde @ "1.0" import Deserialize

# Model type as it would appear in a handler signature: Query[SearchParams]
@derive(Deserialize)
model SearchParams:
    q: str
    limit: int = 10

def main() -> None:
    println("std.web extractor codegen test")
    # In a real handler: async def search(params: Query[SearchParams]) -> ...

